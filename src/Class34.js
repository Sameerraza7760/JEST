import React from "react";

function Class34() {
  let login = true;
  return (
    <div>
      <h1>Get by query and get all by query method</h1>
      <div>{login ? <button>Logout</button> : <button>Login</button>}</div>
    </div>
  );
}

export default Class34;
